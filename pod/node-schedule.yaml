apiVersion: v1
kind: Pod
metadata:
  name: pod-3
spec:
  nodeSelector:
    # kubernetes.io/hostname: k8s-node1
    kubernetes.io/hostname: minikube
  containers:
    - name: container
      image: tmkube/init
